{
  "name": "rock",
  "version": "0.1.0",
  "private": true,
  "description": "A voice agent rendered as a boulder, driven by an xAI Grok speech-to-speech session",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env src/server/index.js",
    "preview": "npm run build && npm start",
    "test": "node --test \"test/**/*.test.js\"",
    "lint": "eslint ."
  },
  "dependencies": {
    "three": "^0.185.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "vite": "^6.0.7"
  }
}
